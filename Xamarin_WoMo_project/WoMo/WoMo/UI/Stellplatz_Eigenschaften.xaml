<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="WoMo.UI.Stellplatz_Eigenschaften">
  <ContentPage.Padding>
    <OnPlatform x:TypeArguments="Thickness"
                iOS="20, 40, 20, 20"
                Android="20, 20, 20, 20"
                WinPhone="20, 20, 20, 20" />
  </ContentPage.Padding>
  <ContentPage.Content>
    <ScrollPane>
      <StackPane>
        <Label x:Name="LblTitle"/>
        <StackPane Orientation="Horizontal">
          <Label Text="Bezeichnung:"/>
          <Entry x:Name="TxtBezeichnung"/>
        </StackPane>
        <StackPane Orientation="Horizontal">
          <Label Text="Position:"/>
          <Entry x:Name="TxtPosition"/>
        </StackPane>
        <StackPane Orientation="Horizontal">
          <Label Text="Bilder:"/>
          <Button x:Name="BtnHinzuBild" Clicked="OnBtnHinzuBildClicked" Text="+"/>
        </StackPane>
        <Grid>
          
        </Grid>
        <ListView x:Name="ListAdapter"/>
        <Button x:Name="HinzuEintrag" Clicked="OnBtnHinzuEintragClicked" Text="+"/>
      </StackPane>
    </ScrollPane>
  </ContentPage.Content>
</ContentPage>